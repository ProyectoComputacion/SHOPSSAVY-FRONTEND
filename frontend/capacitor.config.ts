import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shopssavy.app',
  appName: 'app-shopssavy',
  webDir: 'dist/frontend/browser',

};

export default config;
